import React, { useState } from 'react';

const ClientCard = ({ title, description }) => {
  return (
    <div className="client-colab-card">
      <h4>{title}</h4>
    </div>
  );
};

export default ClientCard;
